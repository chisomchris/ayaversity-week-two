const { accounts } = require("../utils/constants");
const {
  vault,
  deposit,
  myBalance,
  register,
  balanceOf,
  getUser,
  transfer,
  withdraw,
} = require("./functions");
// register();
// register(accounts[1].private);
// register(accounts[2].private);
// deposit(accounts[1].private, "4")
// vault();
// myBalance();
// balanceOf(accounts[1].public);
// getUser(accounts[1].public);
// transfer(accounts[1].private, accounts[0].public, "2");
// balanceOf(accounts[1].public);
// withdraw(accounts[1].private, '1')
// balanceOf(accounts[1].public);
// deposit(accounts[1].private, "5");
// balanceOf(accounts[1].public);
